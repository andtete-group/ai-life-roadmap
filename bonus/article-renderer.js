const params=new URLSearchParams(location.search),articleNo=Number(params.get("no")),article=window.AILifeBonusArticles?.[articleNo],root=document.querySelector("#articleRoot");
const escText=value=>String(value).replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
if(!article){root.innerHTML='<section class="simple-error"><p>記事が見つかりませんでした。</p><a href="./">実践ブログへ戻る</a></section>'}else{
document.title=`${article.title.replace("\n"," ")} | AI LIFE ACADEMY`;const promptId=`prompt-${articleNo}`,articleDate=articleNo===23?"2026.08.22":"2026.08.18",articleDateIso=articleDate.replaceAll(".","-");
root.innerHTML=`<section class="article-hero dynamic-hero" data-article-no="${String(articleNo).padStart(2,"0")}"><div class="article-hero__inner"><div class="article-meta light"><span>${escText(article.code)}</span><time datetime="${articleDateIso}">公開 ${articleDate}</time><span>読了目安 10分</span></div><p class="series-label">AI LIFE実践ブログ｜ARTICLE ${String(articleNo).padStart(2,"0")}</p><h1>${escText(article.title).replace("\n","<br>")}</h1><p>${escText(article.lead)}</p><div class="update-note dark-note"><span class="pulse"></span><strong>${articleDate} 更新</strong><span>内容は更新日時点の情報です。</span></div></div></section>
<div class="article-layout compact-article"><aside class="toc"><p>CONTENTS</p><a href="#overview">全体像</a><a href="#outcomes">完成するもの</a>${article.actionKit?'<a href="#guided-action">実行ミッション</a>':''}<a href="#steps">実践ステップ</a>${article.deepDive?'<a href="#deep-dive">実践を深掘り</a><a href="#case-study">具体例</a><a href="#scorecard">合格基準</a>':''}${article.gallery?'<a href="#real-example">実際の制作例</a>':''}<a href="#case">Before／After</a><a href="#pitfalls">失敗と改善</a><a href="#check">確認リスト</a><a href="#prompt">プロンプト</a>${article.faq?'<a href="#faq">よくある質問</a>':''}<a href="#action">実践課題</a></aside><article class="article-content">
<section class="chapter-section" id="overview"><p class="chapter-no">OVERVIEW</p><h2>最初に押さえる<br><mark>この講座の結論</mark></h2><p class="section-lead">${escText(article.summary)}</p><div class="topic-tags article-tags">${article.tags.map(t=>`<span>${escText(t)}</span>`).join("")}</div></section>
<section class="chapter-section" id="outcomes"><p class="chapter-no">YOUR OUTPUT</p><h2>読み終えたときに<br><mark>完成しているもの</mark></h2><div class="output-grid">${article.outcomes.map((o,i)=>`<div><span>${String(i+1).padStart(2,"0")}</span><strong>${escText(o)}</strong></div>`).join("")}</div><blockquote><b>読むだけで終わらせません。</b><br>この章では、最後の実践課題まで進むことで、自分の仕事に残る成果物を作ります。</blockquote></section>
${article.actionKit?`<section class="chapter-section guided-action" id="guided-action"><p class="chapter-no">DO THIS IN ORDER</p><h2>考えなくて大丈夫<br><mark>上から実行するだけ</mark></h2><div class="mission-head"><div><span>所要時間</span><strong>${escText(article.actionKit.time)}</strong></div><p><b>このミッションのゴール</b>${escText(article.actionKit.goal)}</p></div><div class="mission-steps">${article.actionKit.steps.map((s,i)=>`<article><div class="mission-step-no"><span>STEP</span><b>${i+1}</b><small>${escText(s[0])}</small></div><div class="mission-step-body"><h3>${escText(s[1])}</h3><p>${escText(s[2])}</p><div class="mission-result"><span>ここまでできたらOK</span><strong>${escText(s[3])}</strong></div></div></article>`).join("")}</div><div class="mission-prompt-intro"><span>COPY & START</span><h3>まずは、下の文章をコピーして送る</h3><p>自分で質問や指示を考える必要はありません。AIから質問された内容に、分かる範囲で一問ずつ答えてください。</p></div><div class="mission-prompts"><div class="prompt-card accent-prompt"><div class="prompt-card__top"><span>ChatGPTに送る｜整理・判断・文章作成</span><button class="copy-button" data-copy="mission-chatgpt-${articleNo}">コピーする</button></div><pre id="mission-chatgpt-${articleNo}">${escText(article.actionKit.starterPrompt)}</pre></div><div class="prompt-card"><div class="prompt-card__top"><span>Codexに送る｜ファイル作成・確認</span><button class="copy-button" data-copy="mission-codex-${articleNo}">コピーする</button></div><pre id="mission-codex-${articleNo}">${escText(article.actionKit.buildPrompt)}</pre></div></div><div class="mission-complete"><span>MISSION COMPLETE</span><div><b>完了条件</b><p>5つのSTEPで示した成果物をすべて保存し、「${escText(article.actionKit.goal)}」の状態になったら完了です。</p></div></div></section>`:''}
<section class="chapter-section" id="steps"><p class="chapter-no">PRACTICE</p><h2>迷わず進める<br>実践ステップ</h2><div class="steps">${article.steps.map((s,i)=>`<div class="step"><div class="step-number">${String(i+1).padStart(2,"0")}</div><div><h3>${escText(s[0])}</h3><p>${escText(s[1])}</p></div></div>`).join("")}</div></section>
${article.deepDive?`<section class="chapter-section" id="deep-dive"><p class="chapter-no">DEEP PRACTICE</p><h2>ここまで分かれば迷わない<br><mark>実践を深掘り</mark></h2><p class="section-lead">手順だけでなく、なぜ必要なのか、どこまで行えばよいかを具体的に解説します。</p><div class="deep-dive-list">${article.deepDive.map((d,i)=>`<article><div class="deep-dive-index">0${i+1}</div><div><h3>${escText(d[0])}</h3><p>${escText(d[1])}</p></div></article>`).join("")}</div></section><section class="chapter-section" id="case-study"><p class="chapter-no">REAL SCENARIO</p><h2>具体例で理解する<br><mark>${escText(article.caseStudy.title)}</mark></h2><div class="case-study-card"><div class="case-study-problem"><span>PROBLEM</span><p>${escText(article.caseStudy.problem)}</p></div><ol>${article.caseStudy.process.map((p,i)=>`<li><b>${i+1}</b><span>${escText(p)}</span></li>`).join("")}</ol><div class="case-study-result"><span>完成状態</span><p>${escText(article.caseStudy.result)}</p></div></div></section><section class="chapter-section" id="scorecard"><p class="chapter-no">PASS CRITERIA</p><h2>感覚で終わらせない<br><mark>5つの合格基準</mark></h2><p class="section-lead">各項目を「できた／できていない」で判定し、弱い一箇所だけを改善してください。</p><div class="scorecard">${article.scorecard.map((s,i)=>`<article><span>${String(i+1).padStart(2,"0")}</span><div><h3>${escText(s[0])}</h3><p>${escText(s[1])}</p></div><i>CHECK</i></article>`).join("")}</div></section>`:''}
${article.gallery?`<section class="chapter-section" id="real-example"><p class="chapter-no">REAL EXAMPLE</p><h2>AI LIFEで実際に作った<br><mark>制作画面と完成例</mark></h2><p class="section-lead">説明だけでは分かりにくい工程を、実際の依頼画面・ルール・完成ページで確認します。</p><div class="example-gallery">${article.gallery.map(g=>`<figure class="example-shot ${escText(g.layout)}"><div class="example-shot__frame"><img src="${escText(g.src)}" alt="${escText(g.alt)}" loading="lazy"></div><figcaption><strong>${escText(g.title)}</strong><span>${escText(g.caption)}</span></figcaption></figure>`).join("")}</div><div class="insight-box"><span>POINT</span><div><h3>PCとスマホの両方を完成条件にする</h3><p>「スマホ対応」だけでは判断が曖昧です。PCと390px前後の実画面を確認し、見出しの折り返し、CTA、横スクロール、指で押せる大きさまで確認します。</p></div></div></section>`:''}
<section class="chapter-section" id="case"><p class="chapter-no">BEFORE / AFTER</p><h2>何がどう変わるのか</h2><div class="case-compare"><article><span>BEFORE</span><p>${escText(article.before)}</p></article><b>→</b><article><span>AFTER</span><p>${escText(article.after)}</p></article></div></section>
<section class="chapter-section" id="pitfalls"><p class="chapter-no">TROUBLESHOOTING</p><h2>よくある失敗と<br><mark>具体的な直し方</mark></h2><div class="pitfall-list">${article.pitfalls.map((p,i)=>`<article><b>${String(i+1).padStart(2,"0")}</b><div><h3>${escText(p[0])}</h3><p>${escText(p[1])}</p></div></article>`).join("")}</div></section>
<section class="chapter-section" id="check"><p class="chapter-no">QUALITY CHECK</p><h2>実行・公開前の<br><mark>最終チェック</mark></h2><ul class="today-list">${article.checklist.map((c,i)=>`<li><span>${i+1}</span>${escText(c)}</li>`).join("")}</ul></section>
<section class="chapter-section" id="prompt"><p class="chapter-no">COPY & USE</p><h2>そのまま使える<br><mark>実践プロンプト3本</mark></h2><p class="section-lead">ChatGPTとCodexの役割に合わせて使い分けてください。</p><div class="prompt-card accent-prompt"><div class="prompt-card__top"><span>${escText(article.promptTitle)}</span><button class="copy-button" data-copy="${promptId}">コピーする</button></div><pre id="${promptId}">${escText(article.prompt)}</pre></div>${article.extraPrompts.map((p,i)=>{const id=`prompt-${articleNo}-${i+2}`;return `<div class="prompt-card"><div class="prompt-card__top"><span>${escText(p[0])}</span><button class="copy-button" data-copy="${id}">コピーする</button></div><pre id="${id}">${escText(p[1])}</pre></div>`}).join("")}</section>
${article.faq?`<section class="chapter-section" id="faq"><p class="chapter-no">FAQ</p><h2>実践前に解消する<br><mark>よくある疑問</mark></h2><div class="faq-list">${article.faq.map((f,i)=>`<details ${i===0?'open':''}><summary><span>Q${i+1}</span>${escText(f[0])}</summary><p>${escText(f[1])}</p></details>`).join("")}</div></section>`:''}
<section class="chapter-section finish-section" id="action"><p class="chapter-no">DO IT NOW</p><h2>今日の実践課題</h2><p>上から順に進めれば、最初の成果物が一つ完成します。</p><ul class="today-list">${article.action.map((a,i)=>`<li><span>${i+1}</span>${escText(a)}</li>`).join("")}</ul><p><b>完了条件：</b> 最後の項目まで実行し、作ったファイルまたは記録を保存できたら完了です。</p></section>
<nav class="article-bottom-nav"><a href="${articleNo===2?'./article-01-codex-start.html':`./article.html?no=${articleNo-1}`}">← 前の記事</a><a href="./">記事一覧</a>${articleNo<23?`<a href="./article.html?no=${articleNo+1}">次の記事 →</a>`:"<span>全23回を読了</span>"}</nav></article></div>`}
